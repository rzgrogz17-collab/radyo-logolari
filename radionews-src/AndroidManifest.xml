<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Network -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <!-- Foreground service (background audio) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />

    <!-- Notifications (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Wake lock -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Bluetooth -->
    <uses-permission
        android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <!-- Audio -->
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <!-- Kayıt: Android 9 ve altı için Müzik klasörüne yazma -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <!-- Widget kontrol broadcast'lerini sadece bu uygulamanın (aynı imza)
         gönderebilmesi için özel izin. Başka uygulamaların widget'ı
         doğrudan broadcast göndererek kontrol etmesini engeller. -->
    <permission
        android:name="com.globalradio.livetuneinogzapp.permission.WIDGET_CONTROL"
        android:protectionLevel="signature" />

    <uses-permission android:name="com.globalradio.livetuneinogzapp.permission.WIDGET_CONTROL" />

    <application
        android:name=".RadyoApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RadyoApp"
        android:usesCleartextTraffic="true"
        tools:targetApi="34">

        <!-- Splash Activity (launcher) -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.RadyoApp.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize" />

        <!-- Country Stations Activity -->
        <activity
            android:name=".CountryStationsActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity"
            android:screenOrientation="portrait" />

        <!-- Settings Activity -->
        <activity
            android:name=".SettingsActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity"
            android:screenOrientation="portrait" />

        <!-- Widget -->
        <receiver
            android:name=".RadioWidget"
            android:exported="true"
            android:permission="com.globalradio.livetuneinogzapp.permission.WIDGET_CONTROL">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.globalradio.livetuneinogzapp.WIDGET_PLAY_PAUSE" />
                <action android:name="com.globalradio.livetuneinogzapp.WIDGET_STOP" />
                <action android:name="com.globalradio.livetuneinogzapp.UPDATE_WIDGET" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_info" />
        </receiver>

        <!-- Media3 Session Service -->
        <service
            android:name="androidx.media3.session.MediaSessionService"
            android:exported="true"
            android:foregroundServiceType="mediaPlayback">
            <intent-filter>
                <action android:name="androidx.media3.session.MediaSessionService" />
                <action android:name="android.media.browse.MediaBrowserService" />
            </intent-filter>
        </service>

        <!-- Radio Player Service -->
        <service
            android:name=".service.RadioPlayerService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback" />

    </application>

</manifest>